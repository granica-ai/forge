---
# Minimal stub for envtest. Source of truth lives in gp1-engine.
# The operator does NOT install or modify this CRD — it is owned by install.yaml.
# This stub exists only so that envtest picks it up and ensureCRDs() can verify its presence.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: forgemaintenancepolicies.forge.granica.ai
spec:
  group: forge.granica.ai
  names:
    kind: ForgeMaintenancePolicy
    listKind: ForgeMaintenancePolicyList
    plural: forgemaintenancepolicies
    singular: forgemaintenancepolicy
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
