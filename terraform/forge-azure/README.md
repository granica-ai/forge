# Forge on Azure — Terraform module

Deploys Granica Forge on Azure (AKS + ADLS Gen2 + Workload Identity) in a single `terraform apply`.

**Full walkthrough:** see [docs/azure/terraform_deployment.md](../../public-forge/docs/azure/terraform_deployment.md).

See `variables.tf` for all inputs and `outputs.tf` for outputs.
